version: 1.0
runtime: python3 
build:
  commands:
    build:
      - pip install pipenv
      - pipenv install --pre --skip-lock -r requirements.txt
run: 
  command: gunicorn server.src.main:app
  network: 
    port: 8000
    env: MY_APP_PORT
